<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.script.mapper.ChatIdMapper">
    <insert id="addChatId" parameterType="com.script.entity.ChatId" >
        INSERT into
        chat(id, user1, user2)
        VALUES (#{id},#{user1.id},#{user2.id})
    </insert>
    <select id="selectChatIdByUser" resultType="com.script.entity.ChatId">
        SELECT *
        FROM chat
        WHERE ( user1 = #{user1} and user2 = #{user2} )
        OR ( user1 = #{user2} and user2 = #{user1})
    </select>
</mapper>